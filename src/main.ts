// Phase 0 scaffold placeholder. Game modules (grid/, weapons/, entities/,
// systems/, render/, ui/, state.ts) land in later phases — see
// docs/KNOWN_ISSUES.md and docs/PROTOTYPE_HANDOFF.md for the porting plan.
const app = document.querySelector<HTMLDivElement>('#app');
if (app) {
  app.textContent = 'Slime TD — scaffold running.';
}
